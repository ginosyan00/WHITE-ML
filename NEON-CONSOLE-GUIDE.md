# 🌟 Руководство по работе с Neon Console

## ✅ База данных готова!

Все **27 таблиц** успешно созданы в вашей Neon базе данных и готовы к использованию.

## 🔗 Прямой доступ к таблицам

**Ваша ссылка на таблицы:**
👉 https://console.neon.tech/app/projects/autumn-term-06749994/branches/br-spring-glitter-ahpgsx9m/tables

## 📋 Список всех таблиц

### Основные таблицы:
- ✅ `users` - пользователи
- ✅ `addresses` - адреса пользователей
- ✅ `products` - товары
- ✅ `product_variants` - варианты товаров
- ✅ `categories` - категории
- ✅ `brands` - бренды
- ✅ `orders` - заказы
- ✅ `payments` - платежи
- ✅ `carts` - корзины
- ✅ `settings` - настройки

### Переводы и локализация:
- ✅ `category_translations` - переводы категорий
- ✅ `brand_translations` - переводы брендов
- ✅ `product_translations` - переводы товаров
- ✅ `attribute_translations` - переводы атрибутов
- ✅ `attribute_value_translations` - переводы значений атрибутов

### Атрибуты и характеристики:
- ✅ `attributes` - атрибуты товаров
- ✅ `attribute_values` - значения атрибутов
- ✅ `product_attributes` - связи товаров с атрибутами
- ✅ `product_variant_options` - опции вариантов товаров

### Платежи:
- ✅ `payment_gateways` - платежные шлюзы
- ✅ `payment_attempts` - попытки платежей
- ✅ `payment_webhook_logs` - логи webhook'ов

### Дополнительно:
- ✅ `order_items` - элементы заказов
- ✅ `order_events` - события заказов
- ✅ `cart_items` - элементы корзин
- ✅ `product_labels` - метки товаров

## 🎯 Как использовать Neon Console

### 1. Просмотр таблиц

1. Откройте ссылку выше
2. Войдите в Neon аккаунт (если требуется)
3. Вы увидите список всех таблиц
4. Кликните на любую таблицу для просмотра

### 2. Добавление данных

**Через UI:**
1. Откройте нужную таблицу
2. Нажмите кнопку **"Insert"** или **"+"**
3. Заполните поля
4. Нажмите **"Save"**

**Через SQL Editor:**
1. Откройте **SQL Editor** в Neon Console
2. Введите SQL запрос:
   ```sql
   INSERT INTO users (id, email, "firstName", "lastName", roles)
   VALUES ('clx123...', 'user@example.com', 'John', 'Doe', ARRAY['customer']);
   ```
3. Нажмите **"Run"**

### 3. Просмотр данных

- Кликните на таблицу
- Используйте фильтры для поиска
- Сортируйте по колонкам
- Экспортируйте данные (если доступно)

### 4. Редактирование данных

1. Откройте таблицу
2. Кликните на строку для редактирования
3. Измените значения
4. Сохраните изменения

### 5. SQL запросы

Используйте **SQL Editor** для:
- Сложных запросов
- Массовых операций
- Аналитики данных
- Создания отчетов

**Примеры запросов:**

```sql
-- Посмотреть всех пользователей
SELECT * FROM users;

-- Посчитать товары
SELECT COUNT(*) FROM products;

-- Найти товары с переводами
SELECT p.id, pt.title, pt.locale
FROM products p
JOIN product_translations pt ON p.id = pt."productId"
WHERE pt.locale = 'ru';
```

## 🔍 Проверка данных

Запустите скрипт для проверки:

```bash
node verify-neon-tables.js
```

Этот скрипт покажет:
- Количество записей в каждой таблице
- Статус подключения
- Ссылку на Neon Console

## 💡 Полезные советы

1. **Используйте SQL Editor** для массовых операций
2. **Экспортируйте данные** перед большими изменениями
3. **Используйте фильтры** для поиска нужных записей
4. **Проверяйте связи** между таблицами (foreign keys)
5. **Следите за индексами** для производительности

## 🔐 Безопасность

- ✅ SSL подключение включено
- ✅ Channel binding активен
- ✅ UTF-8 кодировка настроена
- ✅ Доступ только через ваш Neon аккаунт

## 📞 Поддержка

Если возникают проблемы:
- Проверьте подключение к интернету
- Убедитесь, что Neon база данных активна
- Проверьте правильность connection string в `.env`
- Используйте `verify-neon-tables.js` для диагностики

## 🎉 Готово!

Ваша база данных полностью настроена и готова к использованию. Все таблицы созданы, и вы можете начинать добавлять данные через Neon Console!

